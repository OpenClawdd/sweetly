# Handoff — 2026-07-27 19:51

Paste this file path into a new session to resume.

## Where things stand

**Committed:**
- `b7b5c98` — design spec: `docs/superpowers/specs/2026-07-27-punchy-lyrics-design.md`
- `47ada76` — first implementation plan
- `2bd62f1` — plan rebuilt around **anchored alignment**: `docs/superpowers/plans/2026-07-27-punchy-lyrics.md` ← **the plan to follow**

**Uncommitted:** a lot. Earlier session work (collapse guard in `src/main/lyrics/utils.js`, `sources/custom.js`, `scripts/align_lyrics.py`) plus whatever the workflow wrote. Also 6 pre-existing dirty **vendored** files (`src/components/`, `src/css/`, `src/utils/`) from before this session — flagged but never resolved.

## Workflow: FINISHED. Tasks 1–7 are done and unstaged.

Run `wf_a9bda5c3-96e` / task `wvplpnw3i` completed — 7/7 agents, 0 errors, ~452k subagent tokens.

**All of Tasks 1–7 report `done`.** Verify agent measured:

- `npx vitest run` — **102 passed / 10 files**, 0 failures
- All three Python suites pass; `py_compile` clean
- `npm run build` succeeds (268 renderer modules)
- `npx oxlint` — 0 errors, 3 warnings, all pre-existing in `sources/spotify.js`

**Nothing is committed and nothing is staged** — that was deliberate, so parallel agents couldn't race the git index. **First action next session: commit Tasks 1–7 in dependency order** (utils → fetcher → autoAligner → align_lyrics → plumbing), then go to Task 8.

### Verify agent returned `allGreen: false` — for one reason only

The 6 pre-existing dirty **vendored** files, which predate this whole session and which I flagged at its start. Nothing the workflow did touched them; the agents stayed inside `src/main/`, `src/renderer/` and `tests/`. This is the long-standing unresolved item, not a new regression.

### Two findings from the agents worth reading before trusting Task 4

The Task 4 agent pushed back on the plan, correctly:

1. **The plan's `background()` test helper was broken.** Its non-greedy regex stopped at the first inner `</span>`, so the helper returned `""` for every input — two of the plan's tests could never pass regardless of implementation. The agent fixed the helper and verified the corrected test still catches the old behaviour by temporarily neutralising the guard.
2. **The `isLoneInterjection` clause has no observable effect on emitted TTML.** A line consisting only of ad-libs is already emitted as a bare `<p>` by pre-existing code at `autoAligner.js:125-130`, and the agent verified output is byte-identical whether such a line is classified lead or background. The real win in Task 4 — keeping an ordinary word like "pop" in the lead line — does work and is tested. But if the intent was for lone ad-lib lines to actually *render* as background, that needs a separate change to the `if (!leadSpans)` emission branch, and that word isn't in the `INTERJECTIONS` list either.

Also flagged: `tests/test_align_text.py` and `tests/test_align_windowing.py` already existed as untracked files, and `scripts/align_lyrics.py` already carried uncommitted work from earlier in the session — so some of Task 5 may have been partly present before the workflow ran.

Full per-agent detail: `~/.claude/projects/-Users-noahmendieta-Code-spicy-apple-overlay/cff92a08-4fd6-4647-9dfc-b5b249daefcd/subagents/workflows/wf_a9bda5c3-96e/journal.jsonl`

## What was learned (don't re-derive — this cost a lot of tokens)

Measured against the real library:

- **Apple Music is line-level on all 10 broken tracks. Zero word-level.** No external source has word timings for this catalog — checked AMLL TTML DB (2,472 Apple entries, ~85% CJK, no coverage here), FMHY (only plain-text or line-level LRC), TIDAL (line-level; Monochrome just proxies `api.tidal.com/v1/tracks/{id}/lyrics`), and Manzana (same `amp-api` endpoint the app already calls). **Local alignment is the only path.**
- **Apple text is censored on 7 of 10; LRCLIB is uncensored on all 9 it covers.**
- **LRCLIB covers every broken track except "Marisa Stole the Precious Thing"** (IOSYS) — that one has neither source and must run bare ASR.
- Root cause of the original collapse: `token_i += len(accepted)` consumed tokens at the over-supplied per-window estimate while `cursor` advanced by a 0.5s floor, so the loop exited on token exhaustion ~4s into a 162s track. Fixed, but **anchored alignment supersedes it** — anchoring each line to LRCLIB's known window makes collapse structurally impossible instead of merely detected.

## Open items, not started

1. **Task 8 is a hard gate and needs the user at the machine.** BlackHole routed as output, play "Hard Knock" by slayr from 0:00, ~3 min. Verify with the script in Task 8 Step 4. **If it fails, stop — do not build Part B or regenerate the other 8 tracks on top of a bad file.**
2. **Duet-line cutoff bug** (screenshot: Drake — NOKIA). Opposite-aligned lines overflow the right window edge and get clipped. Investigation so far: `Mixed.css:605-620` gives `.SpicyLyricsScrollContainer.HasDuetLines .line.OppositeAligned:not(.rtl)` a `padding-left: 15cqw`, and `#app` in `src/renderer/index.html` is the `container-type: size` context — so `cqw` resolves against the **whole window**, not the lyrics column, while the artwork/NowBar occupies the left ~42%. Not confirmed. **Needs live measurement in the running app, not more CSS reading** — this codebase has already burned two sessions on CSS bugs that looked like animation bugs. Any fix must be host-side (`src/renderer/styles/punch.css` or `index.html`); `Mixed.css` and `ContentBox.css` are vendored and off limits.
3. **Feature request, not yet designed:** a setting to keep line-by-line songs rendering line-by-line, rather than forcing them into word-level TTML whose timings are sometimes inaccurate. This is effectively an opt-out of plan **Task 11** (which splits line-level TTML into evenly-spaced synthesized words) and possibly of the aligner for such tracks. The user's instinct is sound — synthesized word timings are guesses that land off-syllable. Wants brainstorming before implementation.

## Standing constraints

- **Never edit `src/components/`, `src/css/`, `src/utils/`** — vendored AGPL fork of Spicy Lyrics 6.2.3. Host-side changes only (`src/main/`, `src/preload/`, `src/renderer/`).
- npm, not bun. `npx vitest run` for JS; `python3 tests/test_*.py` for Python.
- Upstream modules are not pure — a replacement must port store writes, interval starts and `classList` mutations, not just return values.
